# API base used by the frontend; set to your Mulesoft sandbox or mock
VITE_API_BASE=http://localhost:3000
# If your API uses an API key, set it here; the client will send it as 'x-api-key'
VITE_API_KEY=
